<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fly.xysimj.jasminediary.mapper.UserMapper">
    <select id="getAllUser" resultType="fly.xysimj.jasminediary.entity.User">
        select * from user;
    </select>


        <insert id="insert">
            INSERT INTO user(id,username,password) VALUES (#{id},#{username},#{password})
        </insert>
</mapper>
